{
  "name": "tac",
  "version": "0.0.1",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup_dev": "npm run setup_api && npm run setup_calendar",
    "setup_api": "cd api && npm install",
    "setup_calendar": "cd calendar && npm install",
    "dev": "concurrently \"cd api && npm run dev\" \"cd calendar && npm start\" "
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "dependencies": {
    "zxcvbn": "^4.4.2"
  }
}
